# 基于特征约束生成模型的主轴振动信号数据扩充方法

## 摘要

主轴系统的振动状态与机床加工精度密切相关，稳定、充分的振动样本是开展精度退化建模和状态评估的重要数据基础。实际采集过程中，机床连续运行实验周期较长，单一工况下可获得的有效样本数量有限，直接依赖原始样本训练后续模型容易受到样本覆盖范围不足的影响。针对该问题，本文以主轴空载振动信号为研究对象，提出一种基于特征约束生成模型的振动信号数据扩充方法。该方法在 WGAN-GP 生成框架中引入振动特征约束，并采用残差生成结构，使模型在保留真实振动窗口基本形态的同时生成具有合理扰动的新样本。实验以 `X-2` 通道振动数据为对象，采样频率为 10000 Hz，主轴转速为 5000 rpm，窗口长度为 2048 点。通过与传统加噪扩充、VAE 残差扩充和普通 WGAN-GP 方法对比，结果显示，残差特征约束 WGAN-GP 在平均均值相对误差、平均标准差相对误差、JS 散度、Wasserstein 距离和 MMD 距离上均取得较优结果，其中平均均值相对误差为 0.030063%，MMD 距离为 $3.91\times10^{-7}$。实验结果表明，特征约束与残差生成结构能够提高生成振动样本与真实样本分布的一致性，为后续主轴精度退化建模提供更稳定的数据扩充基础。

关键词：主轴系统；振动信号；数据扩充；特征约束；WGAN-GP；残差生成

## 引言

加工中心主轴系统长期处于高速旋转和复杂载荷作用下，其回转精度和振动状态会随运行时间发生缓慢变化。主轴精度退化建模通常需要连续采集运行时间、振动信号和精度测量数据，再建立由运行时间或状态特征到主轴精度的映射关系。与故障诊断中常见的正常/故障分类问题不同，主轴精度退化过程具有渐变性和长期性，早期采集阶段往往只能获得有限数量的健康状态振动样本。若直接使用这些样本训练后续模型，模型可能只学习到局部工况下的振动分布，难以支撑更长时间尺度上的状态外推。

数据扩充为有限样本条件下的振动建模提供了一种可行思路。传统时间序列扩充方法通常通过加噪、幅值缩放、时间平移或窗口切片构造新样本，这类方法实现简单、可解释性较强，在振动信号处理中具有一定实用价值【引用：Iwana 2021，时间序列数据扩充方法综述】。然而，传统扰动方法主要围绕原始样本进行局部变化，缺少对真实数据潜在分布的建模能力。当振动信号中同时存在时域幅值变化、频域能量转移和倍频成分差异时，单纯依靠固定扰动规则难以充分描述样本分布的复杂性。

深度生成模型能够通过学习训练样本的概率分布生成新数据，近年来逐渐用于机械振动信号扩充和故障样本生成。变分自编码器（variational autoencoder，VAE）通过编码器和解码器建立低维潜在空间，在小样本条件下可用于生成近似真实样本的新窗口【引用：Kingma 2014，变分自编码器】。生成对抗网络（generative adversarial network，GAN）通过生成器与判别器的对抗训练学习数据分布，在图像生成和振动信号扩充中均得到应用【引用：Goodfellow 2014，生成对抗网络】。针对普通 GAN 训练不稳定的问题，Wasserstein GAN 及其梯度惩罚形式 WGAN-GP 通过改进距离度量和约束判别器梯度，提高了训练过程的稳定性【引用：Arjovsky 2017，Wasserstein GAN】【引用：Gulrajani 2017，WGAN-GP梯度惩罚】。已有研究表明，GAN 类模型能够在机械故障样本不足时生成与真实振动信号相似的扩充数据，并改善后续故障检测模型的训练效果【引用：Bui 2021，GAN用于振动信号故障数据扩充】。

现有研究多以故障分类准确率为最终目标，生成样本是否有用通常通过分类模型性能提升来验证。本文面向的是主轴振动信号的数据基础扩充，后续服务对象是主轴精度退化建模而非立即进行故障分类。因此，本文更关注生成样本是否能够在时域、频域和统计特征层面保持真实主轴振动信号的分布规律。围绕这一目标，本文构建传统加噪扩充、VAE 残差扩充、普通 WGAN-GP 和残差特征约束 WGAN-GP 四类方法的统一对比框架，并采用均值相对误差、标准差相对误差、JS 散度、Wasserstein 距离、MMD 距离、PCA 特征分布和平均频谱等指标评价生成样本质量。

本文的主要工作体现在三个方面。其一，针对主轴空载振动信号建立统一的数据窗口化和公共特征评价流程，使不同扩充方法的结果具有可比性。其二，在 WGAN-GP 框架中引入振动特征约束，将 RMS、峰值、峰峰值、峭度、频率重心、频带能量和倍频能量等特征纳入生成过程，减少生成样本在物理统计特征上的偏离。其三，采用残差生成结构，以真实振动窗口作为基础样本，仅由生成器学习合理扰动项，从而降低小样本健康振动场景下完全独立生成带来的波形失真风险。

## 数据与评价特征

### 数据来源与窗口化处理

实验数据为主轴空载条件下采集的振动信号，本文使用其中的 `X-2` 通道。采样频率为 10000 Hz，主轴转速为 5000 rpm。为适应深度生成模型输入，并保证后续特征提取的一致性，原始信号按固定长度进行滑动窗口切分，单个窗口包含 2048 个采样点。每个窗口先去除自身均值，以降低直流偏置对振动特征的影响，再用于模型训练和评价。按照当前实验设置，共得到 1746 个真实振动窗口，四类扩充方法均生成同等数量的新窗口，以避免样本数量差异对评价结果造成干扰。

设第 $i$ 个振动窗口表示为

$$
\mathbf{x}_i=\{x_{i,1},x_{i,2},\cdots,x_{i,N}\},\quad N=2048 .
$$

去均值处理后的窗口为

$$
\tilde{x}_{i,n}=x_{i,n}-\frac{1}{N}\sum_{n=1}^{N}x_{i,n}.
$$

这一处理不改变窗口内的相对振动形态，但能够减少不同采集片段之间由于零点漂移或低频偏置带来的影响。对于主轴空载振动信号，窗口长度需要同时覆盖一定数量的旋转周期并保留局部波形细节。5000 rpm 对应的转频约为 83.33 Hz，2048 点窗口在 10000 Hz 采样频率下约为 0.2048 s，能够覆盖多个主轴旋转周期，适合提取倍频能量和频率重心等特征。

### 公共评价特征

为了从不同角度刻画真实样本与生成样本的一致性，本文提取时域统计特征、频域能量特征和倍频相关特征。时域特征包括 RMS、峰值、峰峰值、偏度、峭度和峰值因子。RMS 用于反映窗口整体振动能量，其定义为

$$
x_{\mathrm{rms}}=\sqrt{\frac{1}{N}\sum_{n=1}^{N}\tilde{x}_{n}^{2}} .
$$

峰峰值用于描述振动幅值变化范围，定义为

$$
x_{\mathrm{pp}}=\max(\tilde{x})-\min(\tilde{x}) .
$$

峰值因子用于衡量瞬时冲击成分与整体能量之间的关系，定义为

$$
C_f=\frac{\max \lvert \tilde{x} \rvert}{x_{\mathrm{rms}}+\varepsilon},
$$

其中 $\varepsilon$ 为防止分母为零的极小常数。偏度和峭度分别描述波形幅值分布的不对称性和尖峰程度，对于识别生成样本是否出现异常冲击或过度平滑具有参考意义。

频域特征基于快速傅里叶变换提取。设窗口信号的频谱功率为 $P(f)$，频率重心定义为

$$
f_c=\frac{\sum_f fP(f)}{\sum_f P(f)} .
$$

频率重心反映频谱能量集中位置。本文还计算 0 到 1 kHz、1 到 3 kHz 和 3 到 5 kHz 三个频带的能量占比，其表达式为

$$
R_{[f_1,f_2]}=\frac{\sum_{f_1\leq f<f_2}P(f)}{\sum_f P(f)} .
$$

考虑到主轴转速已知，本文进一步提取一倍频、二倍频和三四倍频附近的能量占比。倍频能量能够反映主轴旋转相关成分在生成样本中是否得到保持。若生成样本仅在时域幅值上接近真实样本，但倍频能量分布明显偏离，则说明模型没有充分学习主轴振动的频域结构。

### 分布一致性评价指标

单个特征的均值和标准差用于评价生成样本是否保持真实样本的集中趋势和离散程度。对于任一特征 $s$，均值相对误差定义为

$$
E_{\mu}(s)=\frac{\lvert \mu_g(s)-\mu_r(s) \rvert}{\lvert \mu_r(s) \rvert+\varepsilon}\times100\% ,
$$

其中 $\mu_r(s)$ 和 $\mu_g(s)$ 分别为真实样本和生成样本在该特征上的均值。标准差相对误差同理计算。除均值和标准差外，本文采用 JS 散度、Wasserstein 距离和 MMD 距离评价分布层面的差异。JS 散度通过比较特征直方图分布衡量两个概率分布的相似性，Wasserstein 距离反映分布之间的搬运代价，MMD 距离则基于核函数比较真实特征集合与生成特征集合在再生核希尔伯特空间中的均值嵌入差异【引用：Gretton 2012，最大均值差异检验】。

## 基于特征约束的残差 WGAN-GP 数据扩充方法

### 对比扩充方法

本文将传统加噪扩充作为基础对照方法。设真实窗口为 $\mathbf{x}$，加噪扩充样本可表示为

$$
\mathbf{x}^{\prime}=\mathbf{x}+\boldsymbol{\eta},\quad \boldsymbol{\eta}\sim\mathcal{N}(0,\sigma^2).
$$

该方法不会大幅改变原始波形结构，因此在均值、标准差和部分分布指标上通常能够取得较小误差。但加噪扩充的生成范围受限于原始窗口附近，其新样本主要来自局部随机扰动，并未显式学习主轴振动样本的潜在分布。

VAE 残差扩充通过编码器将真实窗口映射到潜在空间，再对潜在变量施加扰动，经解码器恢复为振动窗口变化量。与直接从标准正态先验采样相比，残差扩充更适合当前短周期健康振动数据，因为它以真实窗口为基础，减少了独立采样导致的过度平滑或波形失真。其基本形式可写为

$$
\mathbf{x}^{\prime}=\mathbf{x}+\beta\Delta \mathbf{x}_{\mathrm{vae}},
$$

其中 $\Delta \mathbf{x}_{\mathrm{vae}}$ 为 VAE 解码得到的扰动项，$\beta$ 为残差缩放系数。

普通 WGAN-GP 使用随机噪声直接生成振动窗口。与原始 GAN 相比，WGAN-GP 将判别器替换为 critic，并通过梯度惩罚约束 critic 满足 Lipschitz 条件。其优化目标可表示为

$$
\mathcal{L}_{D}=
\mathbb{E}_{\tilde{\mathbf{x}}\sim P_g}[D(\tilde{\mathbf{x}})]
-\mathbb{E}_{\mathbf{x}\sim P_r}[D(\mathbf{x})]
+\lambda\mathbb{E}_{\hat{\mathbf{x}}}
\left(\lVert \nabla_{\hat{\mathbf{x}}}D(\hat{\mathbf{x}}) \rVert_2-1\right)^2 ,
$$

$$
\mathcal{L}_{G}=-\mathbb{E}_{\mathbf{z}\sim P_z}[D(G(\mathbf{z}))].
$$

普通 WGAN-GP 能够生成独立样本，但在缺少条件特征约束时，生成器只通过 critic 的整体判断学习数据分布，部分关键振动特征可能出现偏离。对于主轴振动信号而言，时域幅值、频带能量和倍频成分均具有明确物理含义，仅依靠对抗损失难以保证这些特征同时保持一致。

### 残差特征约束 WGAN-GP

为提高生成样本对真实主轴振动特征的保持能力，本文在 WGAN-GP 框架中引入条件特征约束和残差生成结构。模型不直接从随机噪声生成完整振动窗口，而是随机选取真实窗口作为基础窗口 $\mathbf{x}_b$，同时输入噪声向量 $\mathbf{z}$ 和目标特征向量 $\mathbf{c}$，由生成器输出残差扰动 $\Delta \mathbf{x}$。最终生成样本为

$$
\mathbf{x}_g=\mathbf{x}_b+\alpha G(\mathbf{z},\mathbf{c},\mathbf{x}_b),
$$

其中 $\alpha$ 为残差缩放系数。该结构的核心思想是保留真实主轴振动窗口的主要形态，让生成器集中学习真实样本邻域内的合理变化，而不是在小样本条件下完全重构高维振动波形。

目标特征向量 $\mathbf{c}$ 由真实窗口的公共评价特征构成，包括 RMS、峰值、峰峰值、频率重心、频带能量占比和倍频能量占比等。训练过程中，生成样本经过同一套特征提取函数得到 $\phi(\mathbf{x}_g)$，并与目标特征 $\mathbf{c}$ 计算特征约束损失：

$$
\mathcal{L}_{f}=\left\lVert
\frac{\phi(\mathbf{x}_g)-\boldsymbol{\mu}_{f}}{\boldsymbol{\sigma}_{f}}
-\frac{\mathbf{c}-\boldsymbol{\mu}_{f}}{\boldsymbol{\sigma}_{f}}
\right\rVert_2^2 ,
$$

其中 $\boldsymbol{\mu}_{f}$ 和 $\boldsymbol{\sigma}_{f}$ 分别为训练样本特征均值和标准差。使用标准化后的特征计算损失，可以避免不同量纲特征在训练中权重失衡。残差幅值约束用于抑制生成器产生过大扰动，其形式为

$$
\mathcal{L}_{r}=\lVert G(\mathbf{z},\mathbf{c},\mathbf{x}_b) \rVert_2^2 .
$$

综合对抗损失、特征约束损失和残差幅值约束后，生成器优化目标为

$$
\mathcal{L}_{G}^{*}=\mathcal{L}_{G}+\gamma_f\mathcal{L}_{f}+\gamma_r\mathcal{L}_{r},
$$

其中 $\gamma_f$ 和 $\gamma_r$ 分别控制特征约束和残差约束的权重。通过该目标函数，生成器不仅要使 critic 难以区分真实样本和生成样本，还需要使生成窗口在关键振动特征上接近真实分布。与普通 WGAN-GP 相比，残差特征约束 WGAN-GP 对主轴振动信号的物理统计特征施加了更直接的约束，因此更适合本文面向精度退化建模的数据扩充任务。

## 实验设置

### 数据与参数

实验使用主轴空载振动信号的 `X-2` 通道。采样频率为 10000 Hz，主轴转速为 5000 rpm，窗口长度为 2048 点。真实样本和各扩充方法生成样本数量均为 1746 个。VAE 模型采用残差扩充方式；普通 WGAN-GP 采用无条件生成结构；残差特征约束 WGAN-GP 在 WGAN-GP 基础上加入条件特征输入、特征约束损失和残差扰动输出。所有方法生成样本后均采用同一套特征提取和评价流程，以保证结果可比。

本文采用传统加噪扩充、VAE 残差扩充、普通 WGAN-GP 和残差特征约束 WGAN-GP 四种方法进行对比。传统加噪方法用于检验简单扰动基线的效果；VAE 残差扩充用于评价潜在空间扰动方法在健康振动数据上的适用性；普通 WGAN-GP 用于观察无条件对抗生成模型对主轴振动窗口的学习能力；残差特征约束 WGAN-GP 作为本文重点方法，用于验证特征约束与残差结构对生成质量的影响。

### 图表与评价口径

本文从指标汇总、特征空间分布、平均频谱、典型波形和关键特征箱线图等角度分析扩充效果。指标汇总图用于展示各方法在平均均值相对误差、平均标准差相对误差、JS 散度、Wasserstein 距离和 MMD 距离上的差异。PCA 特征空间分布图用于观察真实样本与生成样本在降维特征空间中的重叠程度。平均频谱图用于判断生成样本是否保持真实振动信号的频域结构。关键特征箱线图用于比较 RMS、峰值、峰峰值、峭度、频率重心和倍频能量等特征的分布范围。

需要说明的是，传统加噪扩充由于直接在真实窗口附近叠加小幅随机扰动，其统计指标往往较好，这并不意味着它具备复杂分布学习能力。评价传统加噪方法时，应将其视为局部扰动基线；评价生成模型时，则需要同时关注其是否能够保持真实分布、是否存在明显特征偏移，以及是否具有区别于简单复制的扩充能力。

## 实验结果与分析

### 多方法总体指标对比

表 1 给出了四种扩充方法在统一评价框架下的结果。可以看出，普通 WGAN-GP 的平均均值相对误差、平均标准差相对误差、JS 散度、Wasserstein 距离和 MMD 距离均明显高于其他方法，说明在无条件生成结构下，仅依靠对抗损失难以稳定保持主轴振动信号的统计特征。VAE 残差扩充相较普通 WGAN-GP 有明显改善，其平均均值相对误差为 2.429743%，MMD 距离为 0.0281825，表明残差结构对保持真实波形主形态具有积极作用，但其标准差相对误差仍达到 12.975154%，生成样本的离散程度与真实样本仍存在差异。

表 1 不同扩充方法的生成样本质量评价结果

| 方法 | 生成样本数量 | 平均均值相对误差/% | 平均标准差相对误差/% | 平均JS散度 | 平均Wasserstein距离 | MMD距离 |
|---|---:|---:|---:|---:|---:|---:|
| 传统加噪扩充 | 1746 | 0.281191 | 0.317354 | 0.003856 | 0.304695 | 3.99e-05 |
| VAE残差扩充 | 1746 | 2.429743 | 12.975154 | 0.070843 | 0.690038 | 2.82e-02 |
| 普通WGAN-GP | 1746 | 7.870116 | 22.609388 | 0.204850 | 9.010992 | 7.58e-02 |
| 残差特征约束WGAN-GP | 1746 | 0.030063 | 0.045721 | 0.001106 | 0.040753 | 3.91e-07 |

残差特征约束 WGAN-GP 在所有综合指标上均取得最小值。其平均均值相对误差为 0.030063%，平均标准差相对误差为 0.045721%，均低于传统加噪扩充。JS 散度、Wasserstein 距离和 MMD 距离分别为 0.001106、0.040753 和 $3.91\times10^{-7}$，说明生成样本在特征分布和整体分布上与真实样本更接近。该结果表明，特征约束损失能够有效抑制生成样本在关键振动特征上的偏移，残差结构则进一步降低了完全独立生成带来的波形失真。

### 时域与频域特征分析

从时域特征看，传统加噪扩充对 RMS、峰值和峰峰值的均值保持较好，这是因为加噪幅值相对较小，样本仍以真实窗口为主体。残差特征约束 WGAN-GP 的时域特征误差进一步降低，说明模型不仅保留了基础窗口的形态，还通过特征约束调整了扰动幅值，使生成样本在整体能量和幅值范围上更接近真实样本。VAE 残差扩充在峰值、峰峰值和峰值因子等特征上误差较大，可能与潜在空间扰动后解码器对局部峰值恢复不足有关。普通 WGAN-GP 在多个幅值统计特征上偏差明显，说明无条件生成器在高维振动窗口上容易出现幅值分布不稳定。

频域特征分析进一步揭示了不同方法的差异。主轴振动信号不仅需要在时域波形上接近真实样本，还应保持频率重心、频带能量和倍频能量的分布规律。普通 WGAN-GP 在一倍频、二倍频和三四倍频能量占比上的误差较大，说明其生成样本虽然可能在局部波形上具有振动形态，但没有充分保持主轴转频相关成分。VAE 残差扩充在部分频带能量特征上有所改善，但仍存在一定偏差。残差特征约束 WGAN-GP 将频带能量和倍频能量纳入目标特征，因而能够在训练过程中直接约束生成样本的频域结构，使生成结果更符合主轴振动信号的物理特征。

### 特征空间分布分析

PCA 特征空间分布可用于观察真实样本与生成样本在多维特征压缩后的整体重叠情况。理想的扩充样本不应远离真实样本主分布，也不应完全坍缩为少量重复点。普通 WGAN-GP 由于缺少条件特征约束，其生成样本在特征空间中更容易出现偏移，表现为与真实样本分布重叠不足。VAE 残差扩充以真实窗口为基础，在特征空间中更接近真实样本，但其生成样本的离散程度仍与真实样本存在差异。传统加噪扩充与真实样本高度接近，但这种接近主要来自局部随机扰动，不代表模型对复杂分布的学习。

残差特征约束 WGAN-GP 在 PCA 特征空间中与真实样本分布具有更高重叠度。结合 MMD 距离和 JS 散度结果可以判断，该方法生成的样本在整体特征分布上更接近真实数据。由于模型采用基础窗口加残差扰动的形式，生成样本保留了真实样本的主结构；同时，特征约束又限制了残差扰动对 RMS、峰值、频率重心和倍频能量等特征的破坏。这一机制使生成样本既不会像普通 WGAN-GP 那样明显偏离真实分布，也比单纯加噪扩充更具有模型学习意义。

### 方法适用性讨论

四种方法的实验结果反映了不同扩充策略的特点。传统加噪扩充的优点是简单、稳定且易于控制，适合作为基础基线，也可用于对真实信号进行小范围扰动。但其本质是围绕原始窗口进行局部随机变化，无法利用数据驱动方式学习主轴振动信号的潜在分布。若后续需要扩大样本覆盖范围或模拟不同运行状态下的振动变化，仅依赖加噪扩充可能不足。

VAE 残差扩充能够通过潜在空间扰动引入更丰富的变化，残差结构缓解了普通 VAE 先验采样容易过度平滑的问题。该方法适合在样本数量有限且信号形态相对稳定的情况下使用，但其生成质量依赖编码器和解码器对局部峰值、频谱能量和随机扰动的表达能力。在本文实验中，VAE 残差扩充整体优于普通 WGAN-GP，但与传统加噪和残差特征约束 WGAN-GP 相比仍存在特征离散程度偏差。

普通 WGAN-GP 作为无条件对抗生成模型，理论上具有独立生成样本的能力，但在当前主轴健康振动数据场景下表现较弱。这一现象与小样本、高维时间序列和频域结构约束不足有关。振动窗口长度为 2048 点，生成器需要同时学习时域幅值变化和频域能量规律；若训练目标只来自 critic 的整体判断，模型可能生成具有振动外观但统计特征不稳定的样本。

残差特征约束 WGAN-GP 的优势来自两方面。残差结构使生成过程从“完全生成一个窗口”转化为“在真实窗口基础上生成合理扰动”，降低了建模难度；特征约束将主轴振动信号中具有物理意义的统计量直接纳入优化目标，使生成器在训练过程中受到更明确的引导。实验结果显示，该方法在综合指标和单特征误差上均表现较好，说明其更适合当前以健康主轴振动信号为对象的数据扩充任务。

需要保持结论边界。本文实验主要证明生成样本与真实样本在时域、频域和统计特征分布上具有较高一致性，这为后续精度退化建模提供了更可靠的数据基础。但数据扩充的最终价值仍应通过下游任务验证。后续可将扩充后的振动特征与主轴径向回转误差、运行时间 $t$ 融合，建立主轴精度预测模型，并比较加入扩充样本前后模型在真实测试数据上的 MAE、RMSE 和 $R^2$。只有当扩充样本能够改善真实测试集上的预测性能时，才能进一步证明其对精度退化建模的实际作用。

## 结论

本文针对主轴振动信号样本有限的问题，提出一种基于残差特征约束 WGAN-GP 的数据扩充方法。该方法以真实振动窗口为基础，通过生成器学习残差扰动，并将 RMS、峰值、峰峰值、频率重心、频带能量和倍频能量等特征纳入约束损失，从而提高生成样本与真实样本在统计特征和频域结构上的一致性。

在主轴空载振动数据上的实验表明，残差特征约束 WGAN-GP 相比传统加噪扩充、VAE 残差扩充和普通 WGAN-GP 取得更优的综合指标。该方法的平均均值相对误差为 0.030063%，平均标准差相对误差为 0.045721%，MMD 距离为 $3.91\times10^{-7}$，说明生成样本在整体特征分布上更接近真实样本。传统加噪扩充虽然在部分指标上表现较好，但其生成机制主要是局部随机扰动，难以体现对复杂数据分布的学习能力。普通 WGAN-GP 在当前小样本振动窗口任务中存在明显特征偏移，表明无条件对抗生成结构不足以保证关键振动特征的一致性。

本文结果说明，将振动特征约束与残差生成结构结合，能够提升主轴振动信号扩充样本的质量。后续研究将进一步开展生成样本非复制性检验，并结合主轴精度标签构建“运行时间—振动特征—主轴精度”的退化预测模型，从下游预测性能角度验证数据扩充对主轴精度退化建模的实际贡献。
